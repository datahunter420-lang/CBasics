/* H2.** Build a **grade management system** using functions:
- `getMarks(int n)` — takes n subject marks as input, returns total
- `getAverage(int total, int n)` — returns average
- `getGrade(float avg)` — returns grade character (A/B/C/D/F)
- `printResult(...)` — prints formatted result
- `main()` — only calls functions, does no calculations itself*/