/* E1. Write a function `square(int n)` that returns the square of a number. Call it from main and print the result.

         E2. Write a function `printStars(int n)` (void) that prints n stars on one line. Call it 3 times with different values.

         E3. Write a function `celsiusToFahrenheit(float c)` that converts and returns the Fahrenheit value. Take input in main, convert using function, print in main */

#include<stdio.h>
/*int square (int n);
int main(){
int x;
 printf("Enter the number: ");
 scanf("%d",&x);
 int y = square(x);
 printf("The square of %d is %d ",x, y);

 return 0;
}

int square (int n){
    return n * n ;
    
}*/

/* int n;
void printstars(int n){
    for (int i = 1 ; i<=n; i++){
        printf("*");
    }

}
int main(){
    int x;
    printf("Enter the number: ");
    scanf("%d",&x);
    printstars(x);
    return 0;

}*/

/*s

float celsiusToFahrenheit(float c) {
    return (c * 9.0 / 5.0) + 32;
}

int main() {
    float celsius;
    printf("Enter Celsius value: ");
    scanf("%f", &celsius);

    float fahrenheit = celsiusToFahrenheit(celsius);

    printf("%.2f°C = %.2f°F\n", celsius, fahrenheit);
    return 0;
}
}*/








