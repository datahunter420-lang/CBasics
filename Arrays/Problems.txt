/*# PART 10 — PRACTICE PROBLEMS

## Easy (5 Problems)

**E1.** Declare an array of 5 integers. Take input from user. Print them in reverse order.

**E2.** Take 10 numbers as input into an array. Count how many are positive, negative, and zero.

**E3.** Take 5 numbers. Find and print the largest and smallest element.

**E4.** Take 6 numbers. Calculate and print their sum and average.

**E5.** Take two arrays of 3 elements each. Create a third array that is their element-wise sum.
```
a = {1,2,3}  b = {4,5,6}  →  c = {5,7,9}
```

---

## Medium (3 Problems)

**M1.** Take an array of 8 numbers. Separate and print even numbers and odd numbers:
```
Input:  {3, 8, 1, 6, 4, 9, 2, 7}
Evens:  8 6 4 2
Odds:   3 1 9 7
```

**M2.** Take N numbers into array. Search for a target number. Print its position if found, "Not found" otherwise.

**M3.** Take 5 student marks. Sort them using bubble sort. Print ranks (1st = highest).

---

## Hard (2 Problems)

**H1.** Take a 3×3 matrix as input. Print:
- The matrix itself
- Sum of each row
- Sum of diagonal elements (top-left to bottom-right)

**H2.** Take an array of N numbers. Remove all duplicates and print the unique elements only.
```
Input:  {1, 3, 2, 3, 1, 5, 2}
Output: {1, 3, 2, 5}
```

---*/